import React, { Component } from "react";
import "./style.css";

const Header = () => {
  return (
    <div className="Header">
      <header className="Pokemon-header container-fluid">
        <h1 className="Pokemon-title">Pokedex</h1>
      </header>
    </div>
  );
};

export default Header;
